version: '3.8'

services:
  api:
    build: .
    ports:
      - "8090:8000"
    environment:
      - DATABASE_URL=sqlite:////Users/konstantinwdk/Documents/Mis programas/apimaker/backend/deployments/pokedex-demo/data.db
      - PORT=8000
    restart: unless-stopped

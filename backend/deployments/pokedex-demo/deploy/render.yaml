services:
  - type: web
    name: pokédex-demo
    env: docker
    repo: https://github.com/your-org/your-repo
    branch: main
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: "8000"
      - key: DATABASE_URL
        value: sqlite:///./data.db
      
      